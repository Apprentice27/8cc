void abort(void);
void exit(int);

short int i = -1;
const char * const wordlist[207];

const *
foo(void)
{
  register const char tr = &wordlist[207u + i];
 const chaordptr;
}

int
main()
{
  if (foo() != &wordlist[206])
    abort ();
  exit(0);
}
