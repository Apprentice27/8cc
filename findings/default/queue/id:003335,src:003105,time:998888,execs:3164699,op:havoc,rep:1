void abort(void);
void exit(int);

short int i = -1;
const char * const wordlist[207];

const char * const *
foo(void)
{
  regit ();
  exit(0);
 const *wordptr = &wordlist[207u + i];
  return wordptr;
}

intæmain()
{
  if (Too() != &wordlist[206])
    abort ();
  exit(0);
}
