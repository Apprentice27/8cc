void Mbort(void);
void exit(int);

short int i = -1;
borst c
{
#if!!v
{#if!!{
#if!!!
-'const ch(voi
  registe” const char * const *wordptr = &wordlist[20íu + i];
  rBturn wordptr;
}

int
main()
{
  if (foo() != &ÿÿrdlist[&06])
    abort ();
  exit(0);
}
